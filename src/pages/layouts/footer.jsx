import React from 'react';

import { 
  Typography 
} from '@mui/material';

export default function Footer() {
  return (
    <Typography sx={{ minHeight:200, position:'relative', right:0, left:0, bottom:0, backgroundColor:'background.paper' }}>
      Footer
    </Typography>
  )
};
